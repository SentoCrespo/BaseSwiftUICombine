# ---------------------------------
# Domain Test target
# ---------------------------------

targets: 
  PlatformPersistenceSecureKeychainTests:

    ## Properties
    platform: iOS
    type: bundle.unit-test
    gatherCoverageData: false 

    ## Sources
    sources: 

      ### Target files
      - path: PlatformPersistenceSecureKeychain
        includes:
          - Tests
          - "**/*_SPEC*.swift"
      
    ## Settings
    settings:
      base:
        SDKROOT: iphoneos
        INFOPLIST_FILE: $(SRCROOT)/PlatformPersistenceSecureKeychain/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.wunderfleet.platform.persistenceSecure.keychain.tests
        USER_HEADER_SEARCH_PATHS: ../CarSharingSpecialisation/ExternSource
        PRODUCT_NAME: PlatformPersistenceSecureKeychainTests
        SKIP_INSTALL: YES

    ## Dependencies
    dependencies:

      ### iOS SDKs
      - sdk: Foundation.framework
      - sdk: XCTest.framework
      
      ### Other targets
      - target: PlatformPersistenceSecure
        embed: false
      
      ### Local Swift Packages
      
