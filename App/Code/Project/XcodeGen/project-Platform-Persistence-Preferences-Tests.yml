# ---------------------------------
# Domain Test target
# ---------------------------------

targets: 
  PlatformPersistencePreferencesTests:

    ## Properties
    platform: iOS
    type: bundle.unit-test
    gatherCoverageData: false 

    ## Sources
    sources: 

      ### Target files
      - path: PlatformPersistencePreferences
        includes:
          - Tests
          - "**/*_SPEC*.swift"
      
    ## Settings
    settings:
      base:
        SDKROOT: iphoneos
        INFOPLIST_FILE: $(SRCROOT)/PlatformPersistencePreferences/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.wunderfleet.platform.persistence.preferences.tests
        USER_HEADER_SEARCH_PATHS: ../CarSharingSpecialisation/ExternSource
        PRODUCT_NAME: PlatformPersistencePreferencesTests
        SKIP_INSTALL: YES

    ## Dependencies
    dependencies:

      ### iOS SDKs
      - sdk: Foundation.framework
      - sdk: XCTest.framework
      
